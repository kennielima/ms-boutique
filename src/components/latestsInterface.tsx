export interface latests {
    dress: string,
    image: string,
    price: number,
    id: string
}

export interface detail {
    ITEM: latests,
    color: string,
    size: string,
    quantity: number,
    
}